services:
  # search_l3s_gateway:
  #   container_name: search_l3s_gateway
  #   image: search_l3s_gateway:1.0.0
  #   build: .
  #   ports:
  #     - "4000:9040"
  #   environment:
  #     - DB_URL=postgresql://postgres:admin@dev-db-l3s:5435/l3s-db
  #   depends_on:
  #     - l3s-db
  #   networks:
  #     - search
  db:
    image: postgres:14
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: SearchDB
    networks:
      - search
networks:
  search: